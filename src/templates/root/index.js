import React from 'react'
import PropTypes from 'prop-types'

const HomePage = props => {
    return (
        <div>
            Im a root page
        </div>
    )
}

HomePage.propTypes = {

}

export default HomePage
